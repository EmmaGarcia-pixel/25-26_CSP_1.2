# Method to add numbers
def add_numbers(one, two, three):
    sum = one + two
    total = sum * three

    return total

# Write a method that takes in 3 numbers
# Add the first two, and multiply the third
# Return the result of the operation

print(add_numbers(6, 7, 8))
